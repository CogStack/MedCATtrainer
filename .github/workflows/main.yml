name: build

on:
  push:
    branches: [ master ]
    tags: [ 'v*.*.*' ]
  pull_request:
    branches: [ master ]


jobs:
  # run tests / lint / etc. before building container image?

  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout master
        uses: actions/checkout@v2
        with:
          ref: 'master'

      - name: Release Tag
        if: github.ref != 'refs/heads/master'
        run: echo "RELEASE_VERSION=${GITHUB_REF#refs/*/}" >> $GITHUB_ENV
      - name: Non Prod Tag
        if: github.ref == 'refs/heads/master'
        run: echo "RELEASE_VERSION=latest" >> $GITHUB_ENV
      - name: Build
        run: |
          docker build -t cogstacksystems/medcat-trainer:{{ env.RELEASE_VERSION }} webapp/.
          docker build -t cogstacksystems/medcat-trainer-nginx:{{ env.RELEASE_VERSION }}  nginx/.
      - name: Run Django Tests
        run: |
          # run tests
          docker run --rm cogstacksystems/medcat-trainer:{{ env.RELEASE_VERSION }} python manage.py test

      - name: Login to DockerHub
        uses: docker/login-action@v1
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Push to DockerHub
        run: |
          docker push cogstacksystems/medcat-trainer:{{ env.RELEASE_VERSION }}
          docker push cogstacksystems/medcat-trainer-nginx:{{ env.RELEASE_VERSION }}
